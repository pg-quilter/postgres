language: c
compiler:
  - gcc
  - clang
notifications:
  email: false

script: test ! -f .pg-quilter-patch-application-failed.sentinel && ./configure --with-libxml --with-openssl --enable-cassert && make check
